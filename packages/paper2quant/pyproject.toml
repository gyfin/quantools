[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "paper2quant"
version = "0.1.0"
description = "Offline research source staging adapters for qmtq"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
